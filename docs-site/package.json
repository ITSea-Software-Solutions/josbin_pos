{
  "name": "josbin-pos-docs",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Josbin POS — developer + user documentation site (VitePress)",
  "scripts": {
    "dev": "vitepress dev --port 5180 --host",
    "build": "vitepress build",
    "build:itsea": "BASE_PATH=/josbin-docs/ vitepress build",
    "preview": "vitepress preview --port 5180"
  },
  "devDependencies": {
    "marked": "^18.0.6",
    "vitepress": "^1.6.4"
  }
}
