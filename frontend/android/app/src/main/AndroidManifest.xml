<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Plugging a receipt printer into the terminal wakes the till app
                and grants it permission for that printer without a dialog.
                device_filter.xml matches USB printer-class devices only, so a
                scanner (HID) or a USB stick never triggers this.
            -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        USB host mode — required to drive a receipt printer plugged straight
        into the terminal. Android only activates the USB host APIs for apps
        that declare this; without it UsbManager.getDeviceList() returns an
        EMPTY map even with the printer connected and powered, so the printer
        picker in Settings → Hardware stays blank. See the Android USB host
        guide: the <uses-feature> declaration is mandatory, not advisory.

        required="false" so the APK still installs on tablets and phones that
        have no host port — those simply print over the network instead.
    -->
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />
</manifest>
