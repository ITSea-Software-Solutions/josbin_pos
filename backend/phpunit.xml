<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            force="true" is CRITICAL on every DB_* entry below.
            Without it, OS-level env vars (set by docker-compose for the
            demo container — DB_DATABASE=josbin_pos_demo) win over these
            values. The test suite then connects to and wipes the demo
            database via RefreshDatabase — destroying seeded demo data
            every time anyone runs `php artisan test`. With force="true"
            the test bootstrap actively overwrites $_ENV / $_SERVER and
            Laravel's env() returns the test value.

            Verified by running tests after this change:
              \DB::connection()->getDatabaseName() === 'josbin_pos_test'
        -->
        <env name="APP_ENV" value="testing" force="true"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- Dedicated josbin_pos_test database with pgcrypto + vector
             extensions pre-installed via docker/postgres-demo/init.
             Never touches josbin_pos_demo. -->
        <env name="DB_CONNECTION" value="pgsql" force="true"/>
        <env name="DB_HOST" value="postgres" force="true"/>
        <env name="DB_PORT" value="5432" force="true"/>
        <env name="DB_DATABASE" value="josbin_pos_test" force="true"/>
        <env name="DB_USERNAME" value="josbin_pos" force="true"/>
        <env name="DB_PASSWORD" value="secret" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
